
'use client';

import { ReactNode } from "react";

// This file is a placeholder. The actual auth logic is in `auth.ts` and `auth/use-user.tsx`
export function AuthProvider({ children }: { children: ReactNode}) {
    return <>{children}</>
}
